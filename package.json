{
  "author": "Leonardo Meireles",
  "dependencies": {
    "dotenv": "^16.5.0",
    "winston": "^3.17.0"
  },
  "description": "TCP Proxy for Laboratory Equipment Communication with ASTM/HL7 Support",
  "devDependencies": {
    "prettier": "^2.8.8"
  },
  "keywords": [
    "laboratory",
    "proxy",
    "astm",
    "hl7",
    "tcp",
    "medical"
  ],
  "license": "ISC",
  "main": "app.js",
  "name": "proxy_for_labs",
  "scripts": {
    "emu:env": "node app.js & node start-emu-environment.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:staged": "prettier --write",
    "start": "node app.js",
    "stop": "pkill -f 'node app.js|node start-emu-environment.js'",
    "stop:all": "pkill node",
    "check:ports": "lsof -i :54000 && lsof -i :7006 && lsof -i :8000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "commonjs",
  "version": "1.0.0"
}
